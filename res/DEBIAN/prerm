#!/bin/bash

set -e

case $1 in
    remove|upgrade)
		INITSYS=$(ls -al /proc/1/exe | awk -F' ' '{print $NF}' | awk -F'/' '{print $NF}')
		rm -f /usr/bin/onedesk
		
		if [ "systemd" == "${INITSYS}" ]; then

			systemctl stop onedesk || true
			systemctl disable onedesk || true
			rm /etc/systemd/system/onedesk.service /usr/lib/systemd/system/onedesk.service || true
			
			# workaround temp dev build between 1.1.9 and 1.2.0
			serverUser=$(ps -ef | grep -E 'onedesk +--server' | grep -v 'sudo ' | awk '{print $1}' | head -1)
			if [ "$serverUser" != "" ] && [ "$serverUser" != "root" ]
			then
				systemctl --machine=${serverUser}@.host --user stop onedesk >/dev/null 2>&1 || true
			fi
			rm /usr/lib/systemd/user/onedesk.service >/dev/null 2>&1 || true
		fi
        ;;
esac

exit 0
